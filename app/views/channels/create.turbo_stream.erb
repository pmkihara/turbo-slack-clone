<%= turbo_stream.append 'channels', @channel %>
<%= turbo_stream.replace 'modal' do %>
  <%= turbo_frame_tag :modal %>
<% end %>
<%= turbo_stream.update 'content', partial: 'channels/channel_content', locals: { channel: @channel } %>
