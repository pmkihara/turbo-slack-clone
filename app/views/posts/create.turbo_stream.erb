<%= turbo_stream.update(@post.reply? ? 'new-reply' : 'new-post', partial: 'form', locals: { postable: @post.postable, post: Post.new }) %>
<% @post.broadcast_append_to @post.postable, target: @post.reply? ? :replies : :posts %>
